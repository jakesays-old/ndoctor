<?xml version="1.0" encoding="utf-8"?>

<Project ToolsVersion="4.0" DefaultTargets="Build" xmlns="http://schemas.microsoft.com/developer/msbuild/2003">
  
  <Target Name="Before" BeforeTargets="Build">
    <message text="Here's the task to execute before the build" importance="high"/>
  </Target>

  <Target Name="After" AfterTargets="Build">
    <message text="Here's the task to execute after the build" importance="high"/>
	<message text="====== $(ConfigurationName) $(Cinfiguration) $(SolutionFileName) /b $(SolutionDir) ======"/>
	<Exec Command="..\tools\CopyPluginsFiles.cmd Debug"/>
  </Target>
  
</Project>